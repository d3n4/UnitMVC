# Controller application
* / application.main
* /gateway application.gateway
* /bind/([a-zA-Z0-9_]+) application.bind

# Controller generator
* /generator/model/([a-zA-Z0-9_]+) generator.gen_model
* /install/([a-zA-Z0-9_]+) generator.install

# Controller install_data
* /install install_data.index